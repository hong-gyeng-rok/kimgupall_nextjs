const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
  theme: {
    extend: {
      fontFamily: {
        chosunGoosu: ["var(--font-goosu)"],
      },
    },
  },
};

export default config;
